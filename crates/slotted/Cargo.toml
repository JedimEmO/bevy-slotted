[package]
name = "slotted"
description = "Facade for slotted: SlottedPlugins, the headless plugin group, prelude and feature flags."
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[features]
# `script-mlua` is on by default; Cargo has no per-target defaults, so a wasm
# build passes `--no-default-features` and re-enables `ui`, `browser`, `packs`.
default = ["ui", "browser", "packs", "script-mlua"]
# Mods and resource packs: discovery, `pack://`, the script lifecycle (Phase 4).
packs = ["ui", "browser", "dep:slotted-packs", "dep:slotted-script"]
# The native Luau runtime, inserted as `ScriptHost` when none exists.
script-mlua = ["packs", "dep:slotted-script-mlua"]
# Widgets, themes and icons. Off for a dedicated server.
ui = ["dep:slotted-ui", "dep:slotted-theme", "dep:slotted-icons"]
# The item and recipe browser overlay (Phase 3).
browser = ["ui", "dep:slotted-browser"]
# Backdrop blur for glass panels (ADR 0003).
blur = ["ui", "slotted-theme/blur"]
# Exclusion-zone highlighter, id tooltips.
dev = ["ui", "slotted-ui/dev", "slotted-browser?/dev"]

[dependencies]
slotted-model = { workspace = true }
slotted-registry = { workspace = true, features = ["std-fs"] }
slotted-ecs = { workspace = true }
slotted-theme = { workspace = true, optional = true }
slotted-ui = { workspace = true, optional = true }
slotted-icons = { workspace = true, optional = true }
slotted-browser = { workspace = true, optional = true }
slotted-packs = { workspace = true, optional = true }
slotted-script = { workspace = true, optional = true }
slotted-script-mlua = { workspace = true, optional = true }
# The headless group needs exactly what ADR 0002 lists; a game adds
# DefaultPlugins on top of these for a windowed build.
bevy = { workspace = true, features = [
    "std",
    "bevy_asset",
    "bevy_camera",
    "bevy_input_focus",
    "bevy_log",
    "bevy_picking",
    "bevy_scene",
    "bevy_state",
    "bevy_text",
    "bevy_ui",
    "bevy_ui_widgets",
    "bevy_window",
    "default_font",
    "ui_picking",
] }

[lints]
workspace = true
